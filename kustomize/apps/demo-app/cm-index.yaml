apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-app-index
  namespace: demo-app
data:
  index.html: |
    <title> Flux CD demo</title> 
    Started by Flux CD. <p> 
    Special env val = ${FLUX_ENV_NAME}
    
